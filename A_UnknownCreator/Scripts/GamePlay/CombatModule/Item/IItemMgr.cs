using System;
using System.Collections;
using UnityEngine;

namespace UnknownCreator.Modules
{
    public interface IItemMgr : IDearMgr
    {    /*
        void PickUpItem(Unit unit, ItemBase item);
        void PickUpItem(Unit unit, ItemBase item, int handIndex = 1);
        ItemBase CreateItem(string itemName);
        ItemBase CreateItemToGround(string itemName, Vector3 pos, Quaternion rot);
        void PlaceItemToGround(ItemBase item, Vector3 pos, Quaternion rot);
        void ClearItem();   */
    }
}